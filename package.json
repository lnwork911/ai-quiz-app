{
  "name": "teacher-quiz-saas",
  "version": "1.0.0",
  "private": true,
  "description": "Phase 1 foundation: Teacher Quiz SaaS (Netlify, Identity, OpenAI, Upstash Redis, Stripe)",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo \"Static site — no build step required\"",
    "lint": "echo \"Add ESLint when ready\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "jsonwebtoken": "^9.0.2",
    "openai": "^4.73.0",
    "stripe": "^17.4.0"
  }
}
